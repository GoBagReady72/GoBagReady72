Ready72 Wireframes v0.4 — Canon-aligned (LIVE PATCH)
----------------------------------------------------
This package makes the HazAssist-style wireframes the default live view.

Files:
- Ready72WireframesV04.tsx  (React component with Scenes 0–2)
- App.tsx                   (mounts v0.4 as the live app)

How to Apply (GitHub web UI):
1) In your repo, open /src/
2) Upload both files from this ZIP into /src/ (overwrite App.tsx)
3) Commit directly to main
4) Vercel will build automatically

Verify:
- https://admin.gobagready72.com/ shows the wireframes v0.4

Rollback:
- Revert the App.tsx change from GitHub history, or replace with your previous App.tsx
- Optional: keep Ready72WireframesV04.tsx in /src/ for future reference